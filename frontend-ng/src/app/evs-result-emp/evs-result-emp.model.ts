/** Tương ứng với EvsResultEmpDto.java (lịch sử kết quả đánh giá của nhân viên đang đăng nhập). */
export interface EvsResultEmpDto {
  evsYear: string;
  evsPerformance: string;
  evsAbility: string;
}
